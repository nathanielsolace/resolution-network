{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://resolutionnetwork.ai/commons/schema/organization.schema.json",
  "title": "Resolution Commons Organization Record",
  "description": "One organization working on a problem domain. Every record must be traceable: at least one source URL that a human can click to verify the record exists. No fabricated entries, ever.",
  "type": "object",
  "required": ["name", "url", "method", "sources", "added", "verification_status"],
  "additionalProperties": true,
  "properties": {
    "name": { "type": "string", "minLength": 2 },
    "url": { "type": "string", "pattern": "^https?://" },
    "hq_country": { "type": ["string", "null"] },
    "regions": { "type": "array", "items": { "type": "string" } },
    "method": {
      "type": "string",
      "description": "Primary approach. Ocean commons values: river-interception, coastal-cleanup, open-ocean-cleanup, tech-hardware, research-data, policy-advocacy, recycling-valorization, community-network"
    },
    "founded": { "type": ["integer", "null"], "minimum": 1800, "maximum": 2100 },
    "scale_note": {
      "type": ["string", "null"],
      "description": "A stated scale claim (tons removed, rivers covered, members). Only present when a listed source states it."
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "pattern": "^https?://" }
    },
    "added": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "verification_status": {
      "type": "string",
      "enum": ["seed-unverified", "resolver-proposed", "resolver-verified", "org-confirmed", "possibly-inactive"]
    },
    "status": { "type": "string", "enum": ["active", "possibly-inactive", "wound-down"] },
    "http_status": { "type": ["integer", "null"], "description": "Last liveness check result for url" },
    "last_verified": { "type": ["string", "null"] }
  }
}
